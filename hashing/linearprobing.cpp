#include <iostream>
using namespace std;

/**
 * 1. set index = h(k)
 * 2. if table location index already contains the key, no need to insert
 *    it. Done!
 * 3. else f table location index is empty, insert key. done
 * 4. else collision. set index = (index + 1) % tablesize
 * 5. if index = h(k), table is full (throw exception or increase table
 *    size.
 * 6. else go to step 2
 *
 **/
class hash_map {









};
